[install]
# Install dependencies from package.json
auto = "auto"

[install.cache]
# Cache directory for packages
dir = "~/.bun/install/cache"

[install.lockfile]
# Save the lockfile
save = true
print = "yarn"

[install.scopes]
# No private scopes configured

[run]
# Use bun for running scripts
bun = true
