/**
 * Board Detail Loading State
 * Shows skeleton while board data is loading
 */

import { PageSkeleton } from '@/components/shared/loading-skeleton';

export default function BoardLoading() {
  return <PageSkeleton />;
}
